# DTC release synchronization — 2026-07-14

This release marker confirms that `main` contains the approved DueToday premium refinements and is intended to trigger a fresh production deployment from the merged source of truth.

Included release state:

- stable non-pulsing navigation loading state;
- DueToday-only progressive disclosure;
- refined lead capture and empty states;
- connected enquiry-to-payment pipeline language;
- explicit pipeline query failures instead of false zero states;
- permanent DTC/TAD product-boundary regression coverage;
- authenticated page benchmark infrastructure retained as a diagnostic.

No product feature category, permission, database migration, pricing rule or TAD operating model is introduced by this marker.