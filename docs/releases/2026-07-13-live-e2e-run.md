# Live role E2E run

Controlled cookie-jar proxy run: Vercel preview authorization is retained server-side while the browser exercises the real TAD and DTC routes.

Final genuine email-confirmation run triggered after the built-in SMTP quota window reopened. The run must leave no test users, applications, businesses, invitations or memberships after evidence collection.
