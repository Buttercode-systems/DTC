# Live role E2E run

The controlled browser suite runs the exact pull-request build inside GitHub Actions while using the live TAD offers and production managed-service infrastructure.

The run must leave no test users, applications, businesses, invitations, memberships or active-workspace changes after evidence collection.
